{
  "oidcc-prompt-none-not-logged-in": "TIMEOUT — provider returns the spec-compliant 302 to redirect_uri with error=login_required (DOT BaseAuthorizationView.handle_no_permission, OIDC Core 1.0 §3.1.2.6); suite-side HtmlUnit 4.11.1 cannot execute the JS that posts the implicit submit, so the test never leaves WAITING. Pinned by tests.test_authorize.TestAuthorizePromptNoneAnonymous.",
  "oidcc-prompt-none-logged-in": "TIMEOUT — provider implements OIDC Core 1.0 §3.1.2.1 prompt=none silently for skip_authorization=True clients (validate_silent_login/validate_silent_authorization in AllianceAuthOAuth2Validator); suite never reaches the second authorize because HtmlUnit 4.11.1 fails the implicit submit on the FIRST callback. Pinned by tests.test_authorize.TestAuthorizePromptNoneAuthenticated.",
  "oidcc-server": "TIMEOUT — happy-path code-flow does not finish in 180s on a fresh JVM. Suspect HtmlUnit cold-start + first browser-driven module timing edge.",
  "oidcc-alternate-happy-flow": "TIMEOUT — alternate ordering of code-flow steps; HtmlUnit 4.11.1 upstream issue.",
  "oidcc-display-page": "TIMEOUT — display=page parameter; HtmlUnit 4.11.1 upstream.",
  "oidcc-prompt-login": "TIMEOUT — prompt=login flow; suite browser thread stalls before token exchange.",
  "oidcc-max-age-1": "TIMEOUT — max_age=1 forces re-auth; HtmlUnit 4.11.1 upstream.",
  "oidcc-login-hint": "TIMEOUT — login_hint parameter; HtmlUnit 4.11.1 upstream.",
  "oidcc-claims-essential": "TIMEOUT — essential claims request via 'claims' parameter; HtmlUnit 4.11.1 upstream.",
  "oidcc-ensure-request-with-acr-values-succeeds": "TIMEOUT — acr_values parameter; HtmlUnit 4.11.1 upstream.",
  "oidcc-codereuse": "TIMEOUT — code-reuse rejection check; HtmlUnit 4.11.1 upstream.",
  "oidcc-codereuse-30seconds": "TIMEOUT — code-reuse-after-30s check; HtmlUnit 4.11.1 upstream.",
  "oidcc-ensure-request-object-with-redirect-uri": "TIMEOUT — request object containing redirect_uri; HtmlUnit 4.11.1 upstream.",
  "oidcc-unsigned-request-object-supported-correctly-or-rejected-as-unsupported": "TIMEOUT — unsigned request object handling; HtmlUnit 4.11.1 upstream.",
  "oidcc-refresh-token": "TIMEOUT — refresh_token grant; suite never reaches token exchange after the browser flow.",
  "oidcc-scope-email": "TIMEOUT — email scope claims probe; HtmlUnit 4.11.1 upstream.",
  "oidcc-scope-profile": "TIMEOUT — profile scope claims probe; HtmlUnit 4.11.1 upstream.",
  "oidcc-userinfo-post-body": "TIMEOUT — POST /userinfo with credentials in body; HtmlUnit 4.11.1 upstream (sibling oidcc-userinfo-post-header passes).",

  "oidcc-scope-address": "Suite SKIPPED — address scope not advertised in discovery; provider does not return address claims.",
  "oidcc-scope-phone": "Suite SKIPPED — phone scope not advertised in discovery; provider does not return phone claims.",
  "oidcc-scope-all": "Suite SKIPPED — depends on address/phone scopes which the provider does not implement."
}
