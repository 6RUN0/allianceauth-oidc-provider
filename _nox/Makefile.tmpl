# AUTOGENERATED FILE. Do not edit by hand.
#
# Source of truth: the TARGETS table in _nox/makefile.py. Every target
# is a thin shim over a nox session (run via uv); the `help` body and
# the .PHONY list are derived from that same table, so they cannot fall
# out of sync with the nox session registry.
#
# Regenerate after editing the table:
#   uv run nox -s makefile
# Drift is gated (CI + pre-commit) by:
#   uv run nox -s makefile_check

.PHONY: {phony}

{body}
